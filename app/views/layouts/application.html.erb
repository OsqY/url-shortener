<!DOCTYPE html>
<html>
  <head>
    <title>UrlShortner</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://unpkg.com/@popperjs/core@2"></script>
    <script src="https://unpkg.com/tippy.js@6"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="mb-4 flex items-center justify-between">
    <h1 class="m-0"><%= link_to "URL Shortener", root_path, class: "font-extrabold no-underline" %> </h1>  
     <div class="flex items-center gap-2">
    <% if user_signed_in? %>
        <%= button_to "Profile", edit_user_registration_path %>
        <%= link_to "Log out", destroy_user_session_path, method: :delete %>
      <%else %>  
        <%= button_to "Login", new_user_session_path %>
        <%= link_to "Sign up", new_user_registration_path %>
      <% end %> 
     </div>
  </nav> 
    <main class="prose mx-auto mt-28 px-5">
      <% if alert %>
        <p class="bg-yellow-100 text-yellow-800 font-medium px-4 py-2 rounded"><%= alert %></p>
      <% end %>
      <% if notice %>
        <p class="bg-blue-100 text-blue-80 font-medium px-4 py-2 rounded"><%= notice %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
